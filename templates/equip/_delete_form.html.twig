<form method="post" action="{{ path('app_equip_delete', {'id': equip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equip.id) }}">
    <button class="btn">Delete</button>
</form>
